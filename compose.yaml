services:
  web:
    build: .
    container_name: student-intervention-container
    env_file:
      - .env
    environment:
      DB_HOST: host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "5000:5000"